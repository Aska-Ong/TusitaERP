<% provide(:title, 'All Items') %>
<h1>All Items</h1>
<p><%= link_to "Add Item", new_item_path ,:class => 'btn btn-primary'%></p>

<%= will_paginate %>
<ul class="items">
    <%= render @items %>
</ul>

<%= will_paginate %>